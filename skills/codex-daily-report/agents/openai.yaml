interface:
  display_name: "Codex 日报与绩效"
  short_description: "从结构化证据生成并校验日报、周报、月报和绩效"
  default_prompt: "Use $codex-daily-report to collect turn-level evidence, create only a WorkItem JSON bundle, validate before any Markdown write, aggregate weekly/monthly/performance reports from validated sidecars, render leadership weekly reports with the fixed categorized format and management-priority ordering, preserve explicit weekly revisions, print validated weekly files byte-for-byte when asked to output them, and send only validated daily reports according to the local profile."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Initialize Project Controls"
  short_description: "Audit and map durable repository controls"
  default_prompt: "Use $init-pro to inspect and audit this repository, review a source-of-truth mapping, preview and apply only approved plans, archive legacy WORKLOG text explicitly when needed, then run structural and evidence-cited semantic review."
